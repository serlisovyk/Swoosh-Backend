# Backend AI Workspace

This directory stores backend-only AI collaboration artifacts.

- `specs/` defines what should change and why.
- `plans/` defines how approved specs are implemented.
- `decisions/` records durable architecture decisions.
- `reviews/` records review and verification notes.
- `templates/` contains lightweight starting points for new specs and plans.

Keep artifacts concise, current, and specific to the server project.
